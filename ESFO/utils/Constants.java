package utils;

public class Constants {
    public static final int NO_OF_TASKS =100; // number of Cloudlets;
    
  
    public static double s=Math.random();
    public static double t=Math.random();
    public static double r=Math.random();
    public static double phi=Math.random()*2*Math.PI;
    public static final double PP = (s*Math.sin(phi)+t*(Math.cos(phi)))*r;
    
}
